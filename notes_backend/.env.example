# Backend port is managed by the platform.
# Database connection variables (provided by platform). Defaults for local dev:
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=notemaster
POSTGRES_PORT=5001
# Optional full DSN (preferred). If set, overrides the pieces above:
# POSTGRES_URL=postgresql://postgres:postgres@localhost:5001/notemaster
